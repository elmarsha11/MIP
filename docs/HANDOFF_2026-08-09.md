# HANDOFF — Estado de MIP al 2026-08-09

Para: quien siga (Claude en una ventana nueva, u otra IA)
De: Juli Marshall + Claude
Cubre: dos días de trabajo, 16 commits, Fases 3 a 6 más el tablero

> **Leer esto antes de tocar código.** No para saber qué hace el código —eso está
> en los README de cada módulo— sino para saber **por qué está así** y qué
> trampas ya nos costaron tiempo.

---

## 1. Qué es MIP y qué NO es

MIP releva los 86 municipios bonaerenses del Gold Standard y produce una base de
conocimiento **auditable**: cada dato con su URL, su fecha, su cita textual y su
nivel de confianza.

**El activo comercial no son las URLs** (son públicas). Es la base compilada, la
metodología y la comparabilidad entre los 86. Eso es lo que UDS le vende a un
intendente: no la URL de su municipalidad, sino dónde está parado respecto de
los otros 85.

**Es confidencial.** Solo Juli y el equipo de UDS. Nunca publicar nada hacia
afuera: ni artifacts, ni repos públicos, ni datos pegados en herramientas de
terceros.

---

## 2. La regla que sostiene todo

**ADR-0009: sin evidencia no hay dato.** Un vacío se registra como vacío, no se
promedia ni se estima. Está implementado como **validadores que levantan
excepción**, no como advertencias: si el código intenta guardar un dato sin
respaldo, el proceso falla.

De ahí salen las otras:

| ADR | Qué dice |
|---|---|
| 0009 | Sin fragmento no hay dato. El vacío se ve |
| 0010 | Máximo 20 URLs por municipio. Es techo, no piso |
| 0011 | La Capa 2 no depende de buscadores: 4 fuentes independientes |
| 0012 | Se admite `http://` — hay municipios sin TLS, y eso **es** un dato |
| 0013 | La base no se destruye a sí misma. Un verificado no lo pisa un vacío |
| 0014 | **La IA propone, el código verifica.** Gemini da valor + cita; el código comprueba que la cita exista literal en la fuente |
| 0015 | El modelo de impacto reemplaza a la constante de $31.001 |

---

## 3. Estado por fase

| Fase | Módulo | Estado |
|---|---|---|
| 3 — Descubrimiento | `src/discovery/` | **Cerrada.** 85/86 con sitio oficial, 708 URLs, 0 sin evidencia |
| 4 — Extracción | `src/extraction/` | **Cerrada.** 375 hallazgos verificados sobre 20 variables |
| 5 — Impacto | `src/impacto/` | Funcional, con rango de 20x por supuestos sin fuente |
| 6 — Territorio | `src/territorio/` | **Censo a medias.** Solo Chascomús completo (201 entidades) |
| — Tablero | `src/tablero/` | Funcional: **8 vistas**, servidor local y HTML autónomo |
| — INDEC | `src/indec/` | Población oficial 2022 de los 86, **y apertura por sexo** |
| — LLM | `src/llm/` | Capa agnóstica: Gemini o DeepSeek según `MIP_LLM_PROVIDER` |
| — Comercial | `src/oportunidades/` | 79 oportunidades en 54 municipios, con cita |
| — Gabinete | `src/gabinete/` | 209 autoridades; **69 de 86 con intendente** |

**Lo que cambió el 2026-08-09 por la tarde** (todo con su README propio):

- **`src/llm/`** — el proveedor de IA es intercambiable. Se midió DeepSeek contra
  Gemini en Fase 4 y encuentra 42% menos evidencia: **Fase 4 sigue con Gemini**.
  Los módulos nuevos sí usan DeepSeek, porque interpretan evidencia ya probada y
  hay que re-correrlos seguido. Ver `docs/VALIDACION_DEEPSEEK_2026-08-09.md`.
- **`src/oportunidades/`** — el motor comercial. MIP registraba bien lo que hay y
  casi nada de lo que falta, y para UDS el prospecto **es la ausencia**. Relee las
  páginas buscando fricción probada.
- **`src/gabinete/`** — intendentes y secretarios desde el Boletín Oficial (SIBOM)
  y el portal, con la fecha del decreto que los prueba.

**Turnos de salud** (la variable de mayor valor comercial): 11 municipios con
canal digital, **12 con ausencia probada por cita textual** (son los prospectos),
63 sin datos.

---

## 4. Cómo correr todo

```bash
python src/discovery/discovery_engine.py --all        # Fase 3, ~3 min
python src/extraction/extraction_engine.py --all      # Fase 4, ~20 min, usa Gemini
python src/territorio/censo.py --all                  # Fase 6, OSM
python src/indec/censo.py --guardar                   # INDEC (población y sexo)
python src/impacto/motor_impacto.py --turnos          # Fase 5
python src/oportunidades/motor.py --all               # comercial, ~11 min
python src/gabinete/motor_gabinete.py --all           # gabinete, ~9 min
python src/tablero/servidor.py                        # tablero -> 127.0.0.1:8765
python src/tablero/generar_html.py                    # HTML autónomo para compartir
python -m unittest discover -s tests                  # 207 tests
```

Lectura sin gastar IA, que es lo que se usa para preparar una reunión:

```bash
python src/oportunidades/motor.py --ranking           # a quién visitar primero
python src/oportunidades/motor.py --ficha Navarro     # qué venderle, con la prueba
python src/gabinete/motor_gabinete.py --ficha Chascomus
python src/gabinete/motor_gabinete.py --cobertura     # y qué falta
```

Banderas útiles: `--reanudar` y `--faltantes` retoman lo que quedó a medias.
`--sin-ia` corre Fase 4 sin gastar cuota.

**Dependencias nuevas:** `pypdf` (boletines oficiales). Claves: `GEMINI_API_KEY`
y `DEEPSEEK_API_KEY`, o los archivos `credenciales.txt` y `deepseek_key.txt`, los
dos en `.gitignore`.

---

## 5. LO PRIMERO QUE HAY QUE HACER

### 5.1 El Gold Standard tiene poblaciones cruzadas entre municipios

Descubierto el 2026-08-09 al incorporar INDEC. **No son diferencias de medición:
son poblaciones reales asignadas al municipio equivocado.**

| Municipio | Gold Standard | INDEC 2022 |
|---|---:|---:|
| Carlos Tejedor | 1.479 | **14.079** |
| Veinticinco de Mayo | 10.783 | **35.563** |
| Saladillo | 13.977 | **35.656** |
| Tapalqué | 35.656 | **10.783** |
| General Alvear | 37.594 | **12.631** |
| Carmen de Areco | 26.897 | **17.499** |

Roque Pérez tiene la población de General Alvear, Saladillo la de Roque Pérez,
Tapalqué la de Saladillo. La mayoría coincide exacto (Brandsen 32.448,
Chacabuco 52.729), así que es un grupo de filas desalineadas.

**Consecuencia:** el modelo de impacto multiplica por población. General Alvear
entraba al ranking con 37.594 habitantes cuando tiene 12.631. **Migrar
`src/impacto/` a población INDEC antes de usar esos números en una propuesta.**

Corre `python src/indec/censo.py` para ver el cruce completo.

### 5.2 El censo territorial quedó a mitad

Solo Chascomús está completo. Overpass (el servicio público de OSM) se satura y
rechaza consultas. Ya hay cadena de dos espejos y caché; hay que reintentar
`python src/territorio/censo.py --all` en un momento de menos carga, o de a
tandas. El caché hace que lo ya hecho no se repita.

---

## 6. Decisiones que esperan a Juli

1. ~~**Intendentes y secretarios.**~~ **RESUELTO el 2026-08-09.** La propuesta de
   Wikidata P6 **no sirve y está medida**: de los 135 partidos bonaerenses solo
   **7 tienen la propiedad**, y algunos vencidos (figuraba Jorge Macri en Vicente
   López, cargo que dejó en 2023). Lo que sí funciona está en `src/gabinete/`:
   Boletín Oficial (SIBOM) y texto del portal.

   El criterio quedó así, y vale para cualquier fuente futura: **el resumen de IA
   de un buscador no es fuente**, porque no dice de qué publicación salió ni de
   cuándo. El Instagram oficial del municipio **sí lo es** — es un canal del
   municipio y tiene fecha. El buscador sirve para *encontrar* la fuente.

   Se validó contra Chascomús: el buscador daba 8 secretarías y 7 coincidían con
   el decreto. La octava (Obras) daba Jorge Marino y el boletín daba Lucas Funes.
   Juli confirmó que el actual es **Funes**: la prensa local estaba atrasada.
2. **Mapa con callejero.** Hoy el mapa es un SVG de puntos con coordenadas
   reales, sin calles de fondo. Poner un callejero real exige tiles de un
   servicio externo, y eso rompe dos cosas acordadas: que el HTML funcione sin
   internet y que MIP no cargue nada de afuera. Es decisión de Juli.
3. **Publicar el tablero para el equipo.** Hoy escucha solo en 127.0.0.1. Que lo
   vea el equipo exige hosting privado y login. No se hace por default.
4. **Los tres supuestos del modelo de impacto.** Se cierran con **una visita a un
   municipio**, no con más código: cronometrar cuánto tarda un vecino en sacar un
   turno, pedirle al hospital el total de consultas anuales, y preguntar qué
   parte son por guardia. Hoy el resultado varía 20 veces entre piso y techo.
5. **Precio de UDS.** No está definido. El modelo calcula al revés: cuánto puede
   justificar pagar cada municipio dado el valor que se le devuelve.

---

## 7. Trampas que ya costaron tiempo — no repetirlas

**Los caches que guardan el fracaso.** Pasó **tres veces**: en las pistas de
Wikidata, en los límites de OSM y casi en Gemini. Un `None` puede significar "no
existe" o "el servicio estaba caído", y guardarlo congela el error para siempre.
**Regla: cachear solo los aciertos.**

**Los espejos regionales no son alternativas.** `overpass.osm.ch` devuelve 200
con JSON válido y **cero elementos** para Argentina. El código lo tomó como
respuesta autoritativa y concluyó "este municipio no existe en OSM" para 83 de
86. Un espejo con cobertura parcial es otra fuente, no un reemplazo.

**Los timeouts hay que pasárselos al cliente.** `TIMEOUT_SEGUNDOS` estaba
definido y nunca se le pasaba al SDK de Gemini. Una corrida quedó **5 horas
dormida** sin avanzar ni fallar. Un motor que corre solo no puede quedarse mudo.

**La cuota de Gemini es por modelo, no por cuenta.** Cuando uno devuelve 429,
rotar al siguiente de la cadena funciona. Y los modelos se retiran sin aviso:
`gemini-2.5-flash` dejó de estar disponible para cuentas nuevas de un día para
el otro.

**Municipios homónimos en otras provincias.** `veinticincodemayo.gob.ar` es de
San Juan, `rivadavia.gob.ar` también, `maipu.gob.ar` es de Mendoza. Todos pasan
un chequeo de título sin problema. `provincia_ajena()` los filtra. Maipú de
Buenos Aires es `maipu-gba.gob.ar`.

**Verificar el valor, no solo la cita.** En variables de texto libre el valor
*es* el dato. Con la cita real *"obras de bacheo en el barrio Belgrano"*, el
valor `"Juan Pérez"` entraba como intendente verificado con confianza Alta.
Ahora el valor tiene que estar dentro de la cita.

**Las farmacias no son centros de salud.** `amenity=pharmacy` trae
`healthcare=pharmacy` y caía en la heurística de salud: 13 farmacias de
Chascomús se contaban como CAPS.

**Colisiones de nombres de módulo.** `schemas.py` y `modelos.py` existían en dos
paquetes y con `sys.path` se pisaban. Por eso Fase 4 usa `modelos.py` y Fase 6
usa `entidades.py`. **Se volvió a morder el 2026-08-09**: llamar `entidades.py`
al módulo nuevo de `src/oportunidades/` rompió los tests de Fase 6 sin tocar una
línea de Fase 6. Ahora hay `comercial.py` y `autoridades.py`. Antes de nombrar un
archivo, `git ls-files src | xargs -n1 basename | sort | uniq -d`.

**La verificación de la cita se queda corta, siempre un escalón más abajo de lo
que hace falta.** Es la misma trampa cuatro veces, y conviene verla como familia:

| Lo que se verificaba | Lo que se coló |
|---|---|
| que la cita **exista** | *"registrate en el siguiente link"* como oportunidad comercial: la cita probaba lo contrario |
| que contenga el **valor** | *"Centro de Salud «Intendente Pedro Carossi»"*: un CAPS bautizado con un intendente anterior |
| que contenga el **nombre** | *"Sergio F. Bordoni"* suelto en una página de gobierno abierto: el modelo dedujo el cargo del maquetado |
| — | y en variables `si`/`no` el valor nunca está en la cita, así que ahí el guard **no aplica** |

Regla: preguntarse *qué afirma esta fila* y verificar **eso**, no que el texto
esté. Un `si` verificado no prueba nada por sí solo.

**Una cita no se recicla.** En el motor comercial, la misma frase servía de
prueba para dos áreas distintas y Carmen de Areco encabezaba el ranking con 2
evidencias contadas como 4. Un ranking inflado manda al equipo comercial al
municipio equivocado.

**El dato sin fecha no distingue "es" de "fue".** El gabinete guardaba la fecha
de la corrida. Jorge Marino y Lucas Funes firmaban los dos como Secretario de
Obras de Chascomús, con citas literales y correctas: lo único que los ordena es
la fecha del decreto. Cuidado además con las fechas que un decreto menciona sin
ser la suya (plazos de obra, vencimientos): daban "decreto del 2029".

**Recortar texto para el prompt tiene dos formas de perder datos.** Un boletín
trae ~70 firmas de apenas 5 personas: sin deduplicar, el presupuesto se llena de
repeticiones y se pierden las carteras que firman poco (3 de 8 secretarías). Y un
recorte afinado para boletines devuelve **vacío** aplicado a un portal, que no
usa fórmulas de decreto. Si el texto entra entero, va entero.

**Validar el JavaScript antes de generar el HTML.** `node --check
src/tablero/web/app.js`. Una vez un `\n` mal escapado dejó la página en blanco.

**El portal municipal casi no tiene datos.** Chascomús devuelve **393 caracteres
de texto** y 2 enlaces: es JavaScript del lado del cliente y MIP no ejecuta JS.
Casi todo lo que se quiere saber (intendente, escuelas, hospitales) **no está en
el portal**: está en OSM, INDEC, SIBOM, Wikidata. Cuando falte un dato, la
pregunta correcta no es "¿cómo lo extraigo mejor?" sino **"¿qué fuente lo
tiene?"**.

---

## 8. Cosas no obvias del diseño

- **El `id_municipio` sale de `Matriz-86-municipios.csv`**, que está en cuarentena
  por ADR-0009. Se usa solo para identidad, nunca para variables. Desde el
  2026-08-07 el Gold Standard tiene su propia columna y la matriz es respaldo.
- **Los IDs son deterministas** (hash de municipio+URL), no uuid4: re-correr no
  duplica filas.
- **El tablero no calcula nada.** Sus botones lanzan los mismos comandos que se
  correrían a mano. Si calculara por su cuenta tendríamos dos MIP.
- **El frontend es uno solo.** `app.js` detecta si los datos vienen por HTTP o
  incrustados en el HTML autónomo y se adapta. Duplicarlo garantizaría que
  diverjan.
- **`credenciales.txt` está en `.gitignore`** y nunca se versionó. La clave se
  lee de `GEMINI_API_KEY` primero. Lo mismo con `deepseek_key.txt` y
  `DEEPSEEK_API_KEY`.
- **El remoto es `github.com/elmarsha11/MIP` y es PRIVADO.** Verificado el
  2026-08-09: la API de GitHub responde 404 sin credenciales. **Antes de cualquier
  push hay que confirmar que sigue siendo privado**, porque lo que se sube es el
  activo comercial de UDS, no código genérico.
- **Cada base tiene un dueño y no se mezclan.** `hallazgos_86.sqlite` guarda
  evidencia de lo que dice el portal y un verificado no se pisa (ADR-0013).
  `oportunidades_86.sqlite` y `gabinete_86.sqlite` guardan **interpretación** y se
  reemplazan enteras en cada corrida: un secretario que se fue tiene que
  desaparecer, no convivir con el nuevo.
- **Los cachés de texto no son evidencia.** `data/processed/*/cache/` está en
  `.gitignore`: se regenera solo. La evidencia vive con su cita y su URL adentro
  de la base.

---

## 9. Próximos pasos, en orden de valor

1. ~~**Migrar el modelo de impacto a población INDEC**~~ **HECHO el 2026-08-10.**
   Eran 4 de los 12 municipios del modelo: General Alvear entraba con 37.594
   habitantes teniendo 12.631. Ahora manda INDEC en `poblaciones_oficiales()`.
2. **Revisar a ojo el ranking comercial.** `python src/oportunidades/motor.py
   --ranking`, y mirar las 3 o 4 primeras fichas. El problema y el nivel de
   fricción son **lectura del modelo**; lo verificado es la cita. Si el criterio
   no es el de UDS, se ajusta en `src/oportunidades/catalogo.py`.
3. **Terminar el censo territorial** de los 86 (§5.2).
4. **Tercera fuente para el gabinete: las redes oficiales.** Faltan 31 intendentes
   y 24 municipios sin ningún dato de cúpula. `TipoFuente.RED_OFICIAL` ya existe
   en el modelo con confianza Baja, pero no está conectada. Citando el post y su
   fecha, nunca el resumen del buscador.
5. **Viviendas por partido** desde INDEC, en la serie de hogares (§10).
6. **Revisar a ojo los prospectos de turnos.** Algunas citas son literales pero
   flojas: 9 de Julio entró citando *"las vacunaciones se realizan por orden de
   llegada"*, que no prueba que no haya turnero. La verificación frena la
   alucinación, **no el error de criterio**.
7. **Segundo nivel de navegación** en el crawler: hoy se lee una página por tipo,
   y eso limita trámites (50/86) y transparencia (44/86).
8. **Líneas de colectivo** desde OSM, para completar transporte.

---

## 10. Lo que Juli pidió y todavía no está

De la ficha por municipio que pidió el 2026-08-09:

| Pedido | Estado |
|---|---|
| Habitantes | Listo (INDEC) |
| Hombres y mujeres | **Listo (86/86).** Estaba en el cuadro 3.2 del mismo FTP que ya se usaba, no en REDATAM como se creía: son 136 hojas, una por partido |
| Viviendas | **Falta.** No está en la serie de población (los cuadros 7 a 9 son índices de envejecimiento); buscar en la de hogares |
| Intendente | **69 de 86**, con cita y fecha de decreto. De los 17 que faltan, 15 no publican boletín en SIBOM |
| Secretarías y secretarios | **46 municipios, 123 secretarios.** Del Boletín Oficial, con el decreto que los nombra |
| Establecimientos educativos por nivel | Listo, del censo OSM (cobertura parcial) |
| Hospitales y CAPS | Listo, del censo OSM |
| Turnos web / WhatsApp / nulo | Listo, con cita |
| Trámites, cobro de tasas, RAFAM, GDE, app | Listo, con cita |
| Tren | Listo (estaciones OSM) |
| Líneas de colectivo | **Falta** |

---

## 11. Contra qué validar

Juli tiene verdad conocida de **Chascomús** y la usó dos veces para detectar
errores que ninguna prueba automática habría encontrado (las farmacias contadas
como CAPS, y los CAPS reales que faltaban). **Cuando se agregue una fuente
nueva, contrastarla contra Chascomús primero.**

Datos que Juli confirmó de Chascomús: 42.628 habitantes (21.899 mujeres, 20.728
varones, 18.555 viviendas), 1 hospital municipal y 7 CAPS, intendente Javier
Gastón, 8 secretarías, usa RAFAM y GDE/GEDO, **no** tiene turnos digitales de
hospital, tiene tren (Roca, ramal Alejandro Korn–Chascomús) y **no** tiene
transporte público interno.

OSM encontró 5 de esos 7 CAPS. Es incompleto y es honesto decirlo.
